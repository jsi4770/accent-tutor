# Google OAuth "Web application" client ID (safe to expose — it ends up in the
# client bundle regardless via the EXPO_PUBLIC_ prefix). The matching client
# secret lives only in server/.env, never here.
EXPO_PUBLIC_GOOGLE_CLIENT_ID=
